<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>For RIDA ❤️</title>

<style>
body {
    margin: 0;
    padding: 0;
    font-family: 'Segoe UI', sans-serif;
    background: linear-gradient(135deg, #ff758c, #ff7eb3);
    overflow: hidden;
    color: white;
    text-align: center;
}

/* Intro Screen */
#intro {
    position: fixed;
    width: 100%;
    height: 100vh;
    background: black;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    z-index: 10;
}

#intro h2 {
    font-size: 40px;
    animation: glow 2s infinite alternate;
}

#intro button {
    padding: 15px 35px;
    font-size: 18px;
    border-radius: 30px;
    border: none;
    background: #ff4e78;
    color: white;
    cursor: pointer;
    transition: 0.3s;
}

#intro button:hover {
    transform: scale(1.1);
}

/* Main */
#main {
    display: none;
    padding: 40px;
    animation: fadeIn 2s ease-in-out;
}

h1 {
    font-size: 50px;
    animation: glow 2s infinite alternate;
}

.message {
    font-size: 22px;
    width: 75%;
    margin: 30px auto;
    line-height: 1.8;
    animation: fadeIn 4s ease-in-out;
}

/* Animations */
@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

@keyframes glow {
    from { text-shadow: 0 0 10px white; }
    to { text-shadow: 0 0 25px pink; }
}

/* Floating Hearts */
.heart {
    position: fixed;
    bottom: -20px;
    font-size: 22px;
    animation: float 6s linear infinite;
}

@keyframes float {
    from { transform: translateY(0); }
    to { transform: translateY(-100vh); }
}
</style>
</head>

<body>

<div id="intro">
    <h2>RIDA 💖</h2>
    <button onclick="start()">Click To Open Your Heart</button>
</div>

<div id="main">
    <h1>Happy Valentine's Day RIDA ❤️</h1>

    <div class="message">
        RIDA… from the moment you stepped into my life, everything began to feel different. 
        Brighter. Warmer. More meaningful.

        You are not just someone special to me — you are the calm in my chaos, 
        the smile that fixes my worst days, and the reason I believe in something deeper.

        Every laugh we share, every conversation, every memory —
        they all mean more than you probably realize.

        This little page can’t hold all the words I truly feel,
        but if there’s one thing I want you to know today…

        It’s that you matter to me.
        More than words.
        More than moments.
        More than distance or time.

        Thank you for being you.
        Thank you for existing.
        And thank you for making my world softer just by being in it.

        Happy Valentine’s Day ❤️
    </div>

    <h3>Forever Yours 💌<br>– Harry</h3>
</div>

<audio id="music" loop>
    <source src="music.mp3" type="audio/mpeg">
</audio>

<script>
function start() {
    document.getElementById("intro").style.display = "none";
    document.getElementById("main").style.display = "block";
    document.getElementById("music").play();
}

/* Floating hearts */
setInterval(function() {
    const heart = document.createElement("div");
    heart.classList.add("heart");
    heart.innerHTML = "❤️";
    heart.style.left = Math.random() * 100 + "vw";
    document.body.appendChild(heart);

    setTimeout(() => {
        heart.remove();
    }, 6000);
}, 300);
</script>

</body>
</html>
